<% if !@rules.nil? %>
<% @rules.each do |rule| %>
  <%= rule.starting_job_id %> <%= rule.field %> <%= rule.operation %> <%= rule.value %> <%= rule.ending_job_id %>
<% end %>
  <%= render partial: 'form' %>
<% end %>
